sudo: required
services:
  - docker

before_install:
  - docker build -t bhavani/multi-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true bhavani/multi-test npm test

after_success:
  - docker build -t bhavani156/multi-client ./client
  - docker build -t bhavani156/multi-nginx ./nginx
  - docker build -t bhavani156/multi-server ./server
  - docker build -t bhavani156/multi-worker ./worker
  #Log in to the Docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #Take those images and push them to docker hub
  - docker push docker.io/bhavani156/multi-client
  - docker push docker.io/bhavani156/multi-nginx
  - docker push docker.io/bhavani156/multi-server
  - docker push docker.io/bhavani156/multi-worker
